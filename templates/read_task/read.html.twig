{% extends 'base.html.twig' %}

{% block title %}Hello {% endblock %}

{% block body %}
    
    <h1>{{ task.title}}</h1>
    <p>{{ task.description}}</p>
    <p>{{ task.status}}</p>
    
{% endblock %}
